const { LIMIT_LENGTH_NAME, LIMIT_LENGTH_PHONE, LIMIT_LENGTH_PASSWORD } = require('./constants');

const { HTTP_STATUS_CODE } = require('./constantsStatus.js');

module.exports = { LIMIT_LENGTH_NAME, LIMIT_LENGTH_PHONE, LIMIT_LENGTH_PASSWORD, HTTP_STATUS_CODE };
