const { Contact, joiSchema, favoriteJoiSchema } = require('./contacts');
const { User, usersJoiSchema } = require('./users');

module.exports = { Contact, joiSchema, favoriteJoiSchema, User, usersJoiSchema };
